---
description: >-
  MariaDB Enterprise Operator 26.06 is a Stable (GA) release of MariaDB
  Enterprise Operator, released on 2026-06-xx
---

# MariaDB Enterprise Kubernetes Operator 26.06

**Release date**: xx June 2026

### Graceful Shutdown Switchover

In production Kubernetes environments, planned maintenance like node drains is a common operation. MariaDB Enterprise Kubernetes Operator 26.06 introduces graceful shutdown switchover, a new feature designed to improve high availability during these events. This capability automatically triggers a primary switchover when a primary pod is gracefully terminated, ensuring that write availability is maintained with minimal disruption. This release focuses on making MariaDB clusters on Kubernetes more resilient to routine administrative tasks.

The Enterprise Kubernetes Operator now automatically handles primary switchovers during graceful shutdowns, for example, when a node is being drained. When the primary MariaDB pod receives a termination signal, a `preStop` hook initiates a switchover to a suitable replica before the pod is terminated. This ensures that the application's write endpoint remains available and minimizes downtime during planned maintenance. The feature is enabled by default and can be configured via the `spec.replication.primary.autoSwitchoverOnGracefulShutdown` field.

Refer to the [replication documentation](../../tools/mariadb-enterprise-operator/topologies/replication/#primary-graceful-shutdown-switchover) for more details.

### Platform and component versions

The current release has been tested with the following versions:

| Platform/Component        | Version      |
| ------------------------- | ------------ |
| Kubernetes                | 1.36         |
| OpenShift                 | 4.18.6       |
| MariaDB Enterprise Server | 11.8.6-3.2   |
| MaxScale                  | 25.10.2      |

{% include "https://app.gitbook.com/s/SsmexDFPv2xG2OTyO5yV/~/reusable/pNHZQXPP5OEz2TgvhFva/" %}

{% @marketo/form formid="4316" formId="4316" %}