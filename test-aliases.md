# Cross-Space Link Test

This file tests if the GitHub Action correctly expands aliases into full GitBook URLs.

### 1. Server Links
- **Reference:** [Server Reference]({server}/reference/mariadb-server-parameters)
- **Knowledge Base:** [KB Article]({server}/kb/en/library)

### 2. MaxScale Links
- **Configuration:** [MaxScale Config]({maxscale}/configuration-guide)
- **Router:** [Router Setup]({maxscale}/routers/readwritesplit)

### 3. Analytics (ColumnStore) Links
- **Architecture:** [Analytics Arch]({analytics}/architecture)

### 4. Galera Cluster Links
- **Getting Started:** [Galera Setup]({galera}/getting-started)

---
*If the action works, all strings above like `{server}` should be replaced with `https://app.gitbook.com/...`.*
