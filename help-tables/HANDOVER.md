# MariaDB Help Tables — Handover Document

## Background

When a user types `HELP` inside the MariaDB command-line client, the server looks up content from internal "help tables." These tables are pre-populated during installation by a SQL file called `fill_help_tables.sql`, which ships with every MariaDB release.

Until June 2025, the content in that SQL file was generated by a legacy Python script that scraped rendered HTML from the MariaDB Knowledge Base (KB). When the documentation moved from the KB to GitBook (backed by Markdown files in Git), HTML scraping became unreliable.

This project replaces the legacy scraper with a new pipeline that reads the Markdown source files directly and produces the same SQL output.

---

## What Changed

| Before (Legacy) | After (New) |
|---|---|
| Source: KB articles rendered as HTML | Source: Markdown files in the `mariadb-docs` Git repo |
| Method: Python script scrapes HTML | Method: Python script parses Markdown directly |
| Location: Separate `mariadb-documentation` repo | Location: Lives alongside the docs in `mariadb-docs/help-tables/` |
| Trigger: Manual | Trigger: Automated CI on every push to the repo |

The output (`fill_help_tables.sql`) is the same format the MariaDB server has always expected — no changes needed downstream.

---

## How It Works (High Level)

```
Docs author edits a Markdown file
        │
        ▼
Push to GitHub triggers CI (automatic)
        │
        ▼
Python script reads ~1,000 Markdown files
and generates fill_help_tables.sql
        │
        ▼
Validator checks the SQL for errors
        │
        ├── Pass → Artifact uploaded, CI green
        │
        └── Fail → Slack notification sent
```

Before a MariaDB release, a team member downloads the generated artifact from GitHub Actions and uploads it to the server repository manually.

---

## Two Workflows

### Workflow A — CI Validation (automatic)

**When it runs:** Every time someone pushes changes to documentation Markdown files or the generator script.

**What it does:** Generates the SQL, validates it, and uploads it as an artifact. This is the canonical output — it doesn't touch any other repository. If something breaks, the team is notified via Slack.

**Purpose:** Catch problems early so they don't pile up before a release.

### Workflow B — Pre-Release Artifact Download (manual)

**When it runs:** Before each MariaDB release, when the updated SQL needs to ship with the server.

**What it does:** A team member downloads the `fill_help_tables` artifact from the most recent successful CI run on the `main` branch and uploads it to wherever it is needed (server repo, release package, etc.).

**How to download the artifact:**
1. Go to the **Actions** tab in the `mariadb-docs` GitHub repo
2. Open the latest successful **Generate Help Tables** run on `main`
3. Scroll to **Artifacts** at the bottom of the run summary
4. Download **fill_help_tables** — this is the ready-to-use SQL file

**Purpose:** Get the updated help tables into the server release without requiring an automated cross-repo workflow.

---

## Release Cadence

The SQL file should be updated before each MariaDB release. The release schedule is roughly:

- **Innovation releases:** early February, May, August, November
- **Previews:** mid-March, June, September, December
- **New major release:** annually (GA in early February)

Before each release window, someone should download the artifact from the latest successful CI run on `main` (see **Workflow B** above) and deliver it to the server team.

---

## What the SQL File Does Inside MariaDB

The `HELP` command does not read the SQL file at runtime. Instead:

1. `fill_help_tables.sql` is executed once during installation (by `mariadb-install-db`)
2. It populates four tables in the `mysql` database: `help_topic`, `help_category`, `help_keyword`, and `help_relation`
3. The `HELP` command queries these tables when a user asks for help

If upgrades do not automatically reload the help tables, only new installations will pick up updated content.

---

## File Layout

All pipeline files live in the `help-tables/` folder at the root of the docs repo:

| File | What it is |
|---|---|
| `markdown_extractor.py` | The script that reads Markdown and produces the SQL file |
| `validate_sql.py` | Checks the generated SQL for common errors |
| `fill_help_tables.sql` | The generated output (recreated on each run) |
| `failed_files.txt` | List of Markdown files that couldn't be processed |
| `HELP_TABLES_PIPELINE.md` | Technical reference for developers |
| `HANDOVER.md` | This document |

The GitHub Actions workflow lives in `.github/workflows/`:

| File | What it is |
|---|---|
| `generate-help-tables.yml` | Workflow A — automatic CI validation and artifact upload |

---

## GitHub Secrets Required

These must be configured in the docs repo's GitHub settings under **Settings → Secrets and variables → Actions**:

| Secret | What it's for |
|---|---|
| `SLACK_WEBHOOK_URL` | Sends a Slack message when CI fails |

---

## Day-to-Day: What You Need to Know

**As a docs author:** Nothing changes for you. Edit Markdown as usual. CI will automatically verify that the help tables can still be generated.

**As a release manager:** Before a release, go to **Actions → Generate Help Tables**, open the latest successful run on `main`, and download the `fill_help_tables` artifact. Upload the SQL file to the server repo or wherever the release process requires it.

**If CI fails:** Check the GitHub Actions log and the Slack notification in `#docs-notifications`. Common causes are unusual Markdown formatting in new or edited docs pages. To generate the `failed_files.txt` list locally, run `python help-tables/markdown_extractor.py` from the repo root — it will write `help-tables/failed_files.txt` after the run.

---

## References

- **Jira:** DOCS-5630
- **Confluence:** Server Help Tables Documentation Improvements
- **Docs repo:** `mariadb-docs`
- **Legacy generator:** `mariadb-documentation/tree/main/help_contents`
- **MariaDB HELP command docs:** [HELP command](https://mariadb.com/kb/en/help-command/)
- **Release schedule:** [MariaDB release dates](https://mariadb.com/kb/en/mariadb-server-release-dates/)
